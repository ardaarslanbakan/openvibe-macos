# Port patches

The active Designer changes are recorded in `designer-macos.patch`, which is
applied by `script/setup_macos.sh` before the build. Keep this patch aligned
with the exact upstream OpenViBE 3.2.0 Designer revision used for a release.

Do not remove or replace upstream license and attribution files.
