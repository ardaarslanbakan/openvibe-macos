# Port patches

The active source changes are maintained in the upstream source checkout under
`work/designer-master` and the macOS wrapper under `work/macos-port` during
development. Before publishing a release, export the relevant source diffs
here so the repository contains an auditable patch set against the exact
upstream OpenViBE 3.2.0 revision.

Do not remove or replace upstream license and attribution files.
